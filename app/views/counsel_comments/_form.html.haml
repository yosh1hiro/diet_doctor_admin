= form_for @counsel_comment do |f|
  - if @counsel_comment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@counsel_comment.errors.count, "error")} prohibited this counsel_comment from being saved:"
      %ul
        - @counsel_comment.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
