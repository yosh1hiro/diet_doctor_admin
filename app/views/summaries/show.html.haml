= render('shared/header_menu')

%div(class="container-fluid")
  %div(class="row" )
    %div(class="col-md-6")
      %h3 集計
  %div(class="row" )
    = render(partial: 'form')
  - unless @summary.nil?
    = render(partial: "#{@summary.required}")